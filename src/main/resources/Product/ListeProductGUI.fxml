<?xml version="1.0" encoding="UTF-8"?>

<?import java.lang.*?>
<?import java.util.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Font?>

<AnchorPane prefHeight="600.0" prefWidth="900.0" stylesheets="/Styles/StyleProduct.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.Controlleurs.ProductControlleur.ListeProductControlleur">

    <!-- Main Container -->
    <VBox spacing="20.0" AnchorPane.topAnchor="0.0" AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0" AnchorPane.bottomAnchor="0.0">
        <padding>
            <Insets top="30.0" left="30.0" right="30.0" bottom="30.0" />
        </padding>

        <!-- Header Section -->
        <HBox alignment="CENTER_LEFT" spacing="20.0" styleClass="header-container">
            <Label styleClass="page-title" text="Liste des Produits">
                <font>
                    <Font name="System Bold" size="24.0" />
                </font>
            </Label>

            <Region HBox.hgrow="ALWAYS" />

            <Button styleClass="btn-add" text="➕ Nouveau Produit" onAction="#goToCreatePage" prefWidth="180.0">
                <font>
                    <Font name="System Bold" size="13.0" />
                </font>
            </Button>
        </HBox>

        <!-- Table Section -->
        <VBox spacing="15.0" styleClass="table-container" VBox.vgrow="ALWAYS">

            <!-- Search Bar -->
            <HBox spacing="10.0" alignment="CENTER_LEFT" styleClass="search-container">
                <TextField fx:id="searchField" promptText="🔍 Rechercher un produit..." prefWidth="300.0" styleClass="search-input" />
                <Button text="Rechercher" styleClass="btn-search" onAction="#handleSearch" />
                <Button text="Réinitialiser" styleClass="btn-reset" onAction="#handleReset" />
            </HBox>

            <!-- TableView with Action Columns -->
            <TableView fx:id="productTable" styleClass="product-table" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colProductId" prefWidth="60.0" text="ID" styleClass="col-id" />
                    <TableColumn fx:id="colCategory" prefWidth="150.0" text="Catégorie" />
                    <TableColumn fx:id="colPrice" prefWidth="90.0" text="Prix (DT)" styleClass="col-price" />
                    <TableColumn fx:id="colDescription" prefWidth="250.0" text="Description" />
                    <TableColumn fx:id="colCreatedAt" prefWidth="110.0" text="Date création" />
                    <TableColumn fx:id="colUpdate" prefWidth="110.0" text="Mise à jour" styleClass="col-action" sortable="false" />
                    <TableColumn fx:id="colDelete" prefWidth="100.0" text="Supprimer" styleClass="col-action" sortable="false" />
                </columns>
                <columnResizePolicy>
                    <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
                </columnResizePolicy>
            </TableView>

            <!-- Pagination Info -->
            <HBox alignment="CENTER" spacing="10.0" styleClass="pagination-info">
                <Label fx:id="totalProductsLabel" text="Total: 0 produits" styleClass="info-label">
                    <font>
                        <Font size="12.0" />
                    </font>
                </Label>
            </HBox>
        </VBox>
    </VBox>

</AnchorPane>
